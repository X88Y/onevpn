IFf85oomjkBVlKP4i8H5TH-TtJj1I7SR9J9K6TYEtZgO7ST5m2TzPWiuCfnllxO8oJniH30xP6m5vYTdppZspJB3qqMGX8XY9Yl0IDe8QRMQGEk-WdwFjN5Wxuw5tZATKgmqdgPuTWnQYmTsFd7XI9IhL0NNjNnKhGLVlhdSbSSnaMKVwlxdSNiLwTtGo-5phKzxeQlCRP99Lk2Spe5qBkQusKFCSlO6GKNXtwxxBWJ1BOyB_VfJNWBQvMFRg_Rg3A7NHspNJlYLwc43TyGsM5xcj6TdiSpknJ
eyJhbGciOiJSUzUxMiIsInR5cCI6IkpXVCIsImtpZCI6IjFrYnhacFJNQGJSI0tSbE1xS1lqIn0.eyJ1c2VyIjoidmYwMDI1NDUiLCJ0eXBlIjoiYXBpX2tleSIsImFwaV9rZXlfaWQiOiI5YTc3OTkwMC05MTk0LTQ3ODMtOWUyYy05MjY5YjYzMjhjZmQiLCJpYXQiOjE3ODEyMTEwMzR9.n413EMaCTfCfRVHro_iv7k2UtNjr3n0TkZ5zbOqcD_Jl-LeagjWSf5UPJX2q-DzlKzT1LVz3GfdyG_Rz0weX6a8ZQeMNs-3NYmlTbtYmE9kSDYJBB2webIZx8KvKHBZE1gugtTY6_uYi3uiyzel4ZHSRBwrIzipfU05IkWHo0pv_DwfwOkK4zFiKL0Zi4Od58KJrcyiyv_415J_jfuz1mPhua3QGUk8cmQ3YAS7qSTZrqa9KwoALR_O-O80sOTz-CxuLL_SCeaQFBRZlfVT6oRsj2k8GGgDWMNrSwEDD62U5j5kiNUO_Q0pgOVLfG5085Byi7eUHi_KvO7f6Uce4U9KbFIJY7fbGQN4vz_lSyA11GkkGLcI57TjHKfEDdPPXcN4iDi2IyukGi6ZByXJVI8_3ng-if0Ls9RSZnIOx0mxL5ISgM-ilZzjBbRr5U3eaSiVYl931P8w-ncUodD9pDJOrDgkZvUkyjjQ8XyKcvLcI77BUP6zjIMub3L8ru61W
eyJhbGciOiJSUzUxMiIsInR5cCI6IkpXVCIsImtpZCI6IjFrYnhacFJNQGJSI0tSbE1xS1lqIn0.eyJ1c2VyIjoiZXQzODIxMTEiLCJ0eXBlIjoiYXBpX2tleSIsImFwaV9rZXlfaWQiOiI0NWZiZDVlMS04NzMxLTQ1NjEtYmFiNC1lNjcxODZjMjYyYzUiLCJpYXQiOjE3ODEwMTQ4Nzd9.MLUSG41ah7Z4BtSzLAaGFt1T9MjRWXvIyWmzF74KlCsI9kR1GpGG0wRMYgyxd2Txbed45dTHBEJSP7IPltkoXbwPTAMgWOX9euiP7yNVnhfTgEn1BOnk25lkqgDNzErLQEcrb3mzJpTiYP43mXC_FAFkdysNd2gOeH1K41Gbz69LQZjQcWfsoj7sGsOHT7uo_8svdUoAVlXJ0oWh7XdMRCid_ZTQN9gQZrJXZJrrygRbYHLnFkxEgsLinWbsb5SrDa2FcU8f1IP6l9FPJQ7YI14HtAHpHUQw_a5epzK-idxr2wWA1KqG-R6CbVX9oBwi4yDF172M52zAg7FE7w7HcKSjxXRJEwCYJ0Gq8LBebwpx6T6mqfIyie8ptpptY2DqOSgWSEi0drdtJA7xI6QeAJKS35wWBkVes8-m7xE7GkMeD6_vTdh8UEGR9PWdZU7LP0R3yb_IGFzYRWxQz5hot5vUq-kBPmcuj9Pc4Vw3p7gDjGREQ4_zDfexnQy47jSA
'''

import ipaddress
import time
import httpx
import sys
import random
from pathlib import Path

def load_cidr_whitelist(file_path):
    print(f"Loading CIDR whitelist from {file_path}...")
    networks = []
    with open(file_path, 'r') as f:
        for line in f:
            line = line.strip()
            if not line or line.startswith('#'):
                continue
            try:
                networks.append(ipaddress.ip_network(line))
            except ValueError as e:
                print(f"Warning: skipping invalid CIDR line '{line}': {e}")
    print(f"Loaded {len(networks)} CIDR networks.")
    return networks

def is_ip_in_whitelist(ip_str, networks):
    try:
        ip = ipaddress.ip_address(ip_str)
        for net in networks:
            if ip in net:
                return True
    except ValueError:
        print(f"Warning: invalid IP address received: {ip_str}")
    return False

def is_ip_connected(ip_item):
    if ip_item.get("resource_id") is not None:
        return True
    if ip_item.get("server_id") is not None:
        return True
    resource = ip_item.get("resource")
    if resource:
        if isinstance(resource, dict):
            r_id = resource.get("id") or resource.get("resource_id")
            if r_id is not None:
                return True
        else:
            return True
    return False

def clean_unused_ips(headers, base_url, networks, token_idx):
    print(f"Token {token_idx}: Fetching all floating IPs to clean up unused ones...")
    try:
        resp = httpx.get(f"{base_url}/api/v1/floating-ips", headers=headers, timeout=30.0)
        if resp.status_code != 200:
            print(f"Warning: Failed to fetch floating IPs for cleanup: {resp.status_code} {resp.text}")
            return
        
        data = resp.json()
        ips_list = data.get("floating_ips") or data.get("ips") or []
        deleted_count = 0
        for ip_item in ips_list:
            ip_str = ip_item.get("ip")
            ip_id = ip_item.get("id")
            if not ip_str or not ip_id:
                continue
            
            connected = is_ip_connected(ip_item)
            whitelisted = is_ip_in_whitelist(ip_str, networks)
            
            if not connected and not whitelisted:
                print(f"Token {token_idx}: IP {ip_str} (ID: {ip_id}) is unused and NOT whitelisted. Deleting...")
                try:
                    del_resp = httpx.delete(f"{base_url}/api/v1/floating-ips/{ip_id}", headers=headers, timeout=30.0)
                    if del_resp.status_code not in (200, 204):
                        print(f"Warning: Failed to delete IP {ip_str} (status code {del_resp.status_code}): {del_resp.text}")
                    else:
                        print(f"Token {token_idx}: IP {ip_str} deleted successfully.")
                        deleted_count += 1
                except Exception as e:
                    print(f"Warning: Error occurred during IP deletion: {e}")
            else:
                reason = []
                if connected:
                    reason.append("connected to a service")
                if whitelisted:
                    reason.append("whitelisted")
                print(f"Token {token_idx}: Keeping IP {ip_str} ({' and '.join(reason)}).")
        print(f"Token {token_idx}: Cleanup finished. Deleted {deleted_count} unused and non-whitelisted IPs.")
    except Exception as e:
        print(f"Warning: Error during floating IPs cleanup: {e}")

def rotate_ip_for_token(token, networks, token_idx):
    headers = {
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json",
        "Accept": "application/json"
    }
    base_url = "https://timeweb.cloud"
    
    # Verify token by getting projects
    try:
        resp = httpx.get(f"{base_url}/api/v1/projects", headers=headers, timeout=30.0)
        if resp.status_code != 200:
            if resp.status_code == 403:
                print(f"Token {token_idx} is forbidden (status code 403). Cleaning up and skipping.")
                clean_unused_ips(headers, base_url, networks, token_idx)
                return
            print(f"Error: Token {token_idx} is invalid (status code {resp.status_code}): {resp.text}")
            sys.exit(1)
        print(f"Token {token_idx}: Connected successfully to Timeweb Cloud.")
    except Exception as e:
        print(f"Error: Connection failed for Token {token_idx}: {e}")
        sys.exit(1)

    zones = ["spb-1", "spb-2", "spb-3", "spb-4", "nsk-1", "msk-1", "msk-1", "msk-1", "msk-1", "msk-1", "msk-1"]
    attempt = 0
    while True:
        attempt += 1
        zone = random.choice(zones)
        print(f"Token {token_idx} (Attempt {attempt}): Creating floating IP in {zone}...")
        
        # 1. Create floating IP
        payload = {
            "availability_zone": zone,
            "comment": f"mvm-rotator-ip-{token_idx}",
            "is_ddos_guard": False
        }
        
        try:
            resp = httpx.post(f"{base_url}/api/v1/floating-ips", headers=headers, json=payload, timeout=30.0)
            if resp.status_code != 201:
                if resp.status_code == 403:
                    print(f"Token {token_idx} returned 403 (daily limit exceeded or forbidden). Cleaning up and skipping.")
                    clean_unused_ips(headers, base_url, networks, token_idx)
                    return
                print(f"Error creating floating IP (status code {resp.status_code}): {resp.text}")
                sys.exit(1)
            
            data = resp.json()
            floating_ip = data.get("floating_ip") or data.get("ip") or {}
            ip_str = floating_ip.get("ip")
            ip_id = floating_ip.get("id")
            
            if not ip_str or not ip_id:
                print(f"Error: Invalid API response format when creating floating IP: {data}")
                sys.exit(1)
                
            print(f"Token {token_idx} (Attempt {attempt}): Created IP {ip_str} (ID: {ip_id})")
        except Exception as e:
            print(f"Error occurred during IP creation: {e}")
            sys.exit(1)
            
        # 2. Check if IP is whitelisted
        whitelisted = is_ip_in_whitelist(ip_str, networks)
        if whitelisted:
            print(f"Token {token_idx} (Attempt {attempt}): Success! IP {ip_str} is in the whitelist. Keeping it.")
            break
            
        # 3. If not whitelisted, delete it and try again
        print(f"Token {token_idx} (Attempt {attempt}): IP {ip_str} is NOT in the whitelist. Deleting...")
        try:
            del_resp = httpx.delete(f"{base_url}/api/v1/floating-ips/{ip_id}", headers=headers, timeout=30.0)
            if del_resp.status_code not in (200, 204):
                print(f"Error deleting floating IP {ip_str} (status code {del_resp.status_code}): {del_resp.text}")
                sys.exit(1)
            print(f"Token {token_idx} (Attempt {attempt}): IP {ip_str} deleted successfully.")
        except Exception as e:
            print(f"Error occurred during IP deletion: {e}")
            sys.exit(1)
            
        # Wait a short duration between rotations to avoid overloading the API
        time.sleep(1.0)

def main():
    script_dir = Path(__file__).resolve().parent
    whitelist_path = script_dir / "cidrwhitelist.txt"
    if not whitelist_path.exists():
        print(f"Error: whitelist file not found at {whitelist_path}")
        sys.exit(1)
        
    networks = load_cidr_whitelist(whitelist_path)
    tokens = [t.strip() for t in timeweb_tokens.strip().split('\n') if t.strip()]
    
    if not tokens:
        print("Error: No tokens found in rotate_ip.py")
        sys.exit(1)
        
    print(f"Found {len(tokens)} tokens. Starting IP rotation process...")
    for idx, token in enumerate(tokens, 1):
        print(f"\n--- Processing Token {idx}/{len(tokens)} ---")
        rotate_ip_for_token(token, networks, idx)
        
    print("\nRotation process completed successfully for all tokens.")

if __name__ == '__main__':
    main()