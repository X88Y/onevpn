Across all code (Dart, Python, TypeScript, etc.), place all import statements at the very start of the file. Do not use local, inline, or deferred imports inside functions, classes, or block scopes.
- **Fallback Modes**: Do not use or implement fallback modes. Code should handle errors explicitly and fail fast rather than falling back to alternative or degraded modes.